#### WEBHOOK aka admission controllers
- intercepts request to kubernetes API server
- can inject changes to request (annotation, label, sidecar) or reject it

#### TYPES
- validation webhook
- mutating webhook